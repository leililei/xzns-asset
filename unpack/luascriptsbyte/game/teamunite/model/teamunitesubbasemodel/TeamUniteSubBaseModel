-----------------------------------------------------------------------
-- E:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.TeamUnite.Model.TeamUniteSubBaseModel	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/08/15
-- Description: 此处填写描述
-----------------------------------------------------------------------
---@class TeamUniteSubBaseModel:GameDataModelBase
local TeamUniteSubBaseModel = _G.BaseClass("TeamUniteSubBaseModel", _G.GameDataModelBase)

---创建对象时,入口构造函数
function TeamUniteSubBaseModel:__init()
    self.seasonId = 0
    ---红点id赋值（默认为0
    self.redPointId = 0
end

---销毁对象时,析构函数
function TeamUniteSubBaseModel:Dispose()
end

--region 子类重写
---从服务器拉取数据
function TeamUniteSubBaseModel:GetDataFormServer()
    self:ParseData()
end

---初始化功能逻辑
function TeamUniteSubBaseModel:InitFunction()
end

---检测功能是否开启
function TeamUniteSubBaseModel:CheckIsOpen()
    return require "TeamUniteModel":GetInstance():CheckSeasonIsOpen()
end

---赛季开始
function TeamUniteSubBaseModel:OnSeasonStart()
end

---赛季结束
function TeamUniteSubBaseModel:OnSeasonEnd()
end

---刷新红点显示
function TeamUniteSubBaseModel:RefreshRedPoint()
    if self.redPointId > 0 then
        require "RedPointManager":GetInstance():RefreshNode(self.redPointId)
    end
end

---打开界面
function TeamUniteSubBaseModel:OpenView(hideFun, openFun, ...)
end

---设置入口额外图标
function TeamUniteSubBaseModel:SetExtraBtn()
    return 0,nil,nil
end

---变换入口额外图标
function TeamUniteSubBaseModel:ExtraOnclick()

end

return TeamUniteSubBaseModel
