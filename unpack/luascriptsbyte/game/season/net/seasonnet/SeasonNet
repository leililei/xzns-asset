-----------------------------------------------------------------------
-- E:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.Season.Net.SeasonNet	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/05/17
-- Description 通信协议
-----------------------------------------------------------------------

local base = require "BaseGameNet"
---@class SeasonNet : BaseGameNet	通信协议
local SeasonNet = _G.BaseClass("SeasonNet", base)

---@return SeasonNet SeasonNet
function SeasonNet:GetInstance()
    return self.super.super.GetInstance(self)
end

---请求
---@param pbData table PB数据
---@param callback function 完成回调
function SeasonNet:GetInfo(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_port_info
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end
--region  赛季boss
---获取赛季Boss排行榜数据
function SeasonNet:GetBossRank(callback)
    local pbData = {page = 1}
    local item = _G.ConfigManager.net_map.game_rank_season_boss
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取赛季Boss玩家排行
function SeasonNet:GetBossPlayerRank(callback)
    local pbData = {type = 18}
    local item = _G.ConfigManager.net_map.game_rank_get_rank_num
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取赛季Boss信息
function SeasonNet:GetBossInfo(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_boss_port_info
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end
---完成Boss任务
function SeasonNet:FinishBossTask(pbData, callback)
    local item = _G.ConfigManager.net_map.game_season_boss_port_finish
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end
---挑战Boos
function SeasonNet:FightBoss(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_boss_port_fight
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
    return port
end

---获取签到奖励
function SeasonNet:GetSignInfo(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_sigh_port_info
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取签到奖励
function SeasonNet:GetSignReward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_season_sigh_port_award
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

--endregion
--region  赛季特权
--获取赛季特权信息
function SeasonNet:SaiJiTeQuan(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_privilege_info
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end
--领取赛季特权奖励
function SeasonNet:LingQuJiangLi(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_privilege_get
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

--endregion
---获取赛季冲锋奖励
function SeasonNet:GetSeasonSpecialAward(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_port_special_award
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取赛季期待奖励
function SeasonNet:GetSeasonExpectAward(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_port_login_award
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取赛季礼包购买次数
function SeasonNet:GetSeasonSuperValueGiftBuyTimes(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_port_season_store
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---领取赛季大礼日常活跃免费礼包
function SeasonNet:GetAward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_season_port_season_store_award
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---获取赛季大礼奖励
function SeasonNet:GetSeasonBigGift(callback)
    local pbData = self:GetEmptyPB()
    local item = _G.ConfigManager.net_map.game_season_port_gift_award
    local port = base.Access(self, item, pbData, _G.handlerbind(self, self.CallBack))
    self.Call[port] = callback
end

---请求回调
---@param port number 流水号
---@param data table 返回PB数据
---@param error string 错误信息
function SeasonNet:CallBack(port, data, error)
    if error then
        return
    end

    --TODO 写通用数据解析，没有需要做的记得删除TODO

    if self.Call[port] then
        self.Call[port](data)
        self.Call[port] = nil
    end
end

---@type SeasonNet
_G.SeasonNet = SeasonNet
return SeasonNet
