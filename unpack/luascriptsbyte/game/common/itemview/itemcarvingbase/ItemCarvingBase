--------------------------------------------------------------------------
-- E:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.Common.ItemView.ItemCarvingBase	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2021/12/25
-- Description: 此处填写描述
-----------------------------------------------------------------------
---@class ItemCarvingBase    符文
---@field data CarvingData
---@field go_table ItemCarvingBase_GoTable
local ItemCarvingBase = _G.BaseClass("ItemCarvingBase", require("ItemViewBase"))

function ItemCarvingBase:__init(obj)
    self.gameObject = obj
    self.go_table = _G.GetAutoGoTable(obj, _G.handler(self, self.OnClickBtn))
end

---@param data CarvingData
function ItemCarvingBase:ShowData(data, click)
    self:Show(data.sid, data.uid, click, data)
    self:ShowRoleHead(false)
end

function ItemCarvingBase:Show(sid, uid, click, data)
    self.sid = sid
    self.uid = uid or 0
    if data then
        self.data = data
    else
        if self.uid and self.uid ~= self.sid and self.uid > 0 then
            self.data = _G.StorageManager:GetInstance():GetItemDataByID(_G.StorageID.Carving, uid)
        else
            self.data = _G.StorageManager:GetInstance():GetItemDataClass(self.sid, 1)
        end
    end
    self.click = click
    self:ShowInfo()
end

function ItemCarvingBase:ShowInfo()
    self.go_table.aorimage_Color2.SpriteName = _G.CarvingRankEnum[self.data.Grade]
    self.go_table.aorrawimage_Icon:LoadImage(self.data.Icon)
    ---套装角标
    -- local cfg = _G.ConfigManager.carving_suit_cfg[self.data.suit]
    -- if _G.string.IsNullOrEmpty(cfg.icon) then
    --     self.go_table.aorrawimage_suit:SetActiveE(0)
    -- else
    --     self.go_table.aorrawimage_suit:SetActiveE(1)
    --     self.go_table.aorrawimage_suit:LoadImage(cfg.icon)
    -- end
    self.go_table.obj_up:SetActiveE(0)
    self.go_table.aorimage_LvBg:SetActiveE(0)
end

function ItemCarvingBase:HidePart()
    --self.go_table.aorrawimage_suit:SetActiveE(0)
end

function ItemCarvingBase:ShowSlotLevel(isShow, lv)
    self.go_table.aorimage_LvBg:SetActiveE(isShow and 1 or 0)
    if isShow then
        self.go_table.aortext_buildLv.text = lv or 1
    end
end

function ItemCarvingBase:ShowLevel(isShow, level)
    self.go_table.aortext_level:SetActiveE(isShow and 1 or 0)
    if isShow then
        local lv = level and level or self.data.level
        self.go_table.aortext_level.text = _G.GetLangPackValue(32001170403, lv)
    end
end

function ItemCarvingBase:ShowRoleHead(isShow)
    local cardData = self.data.cardUid ~= 0 and _G.StorageManager:GetInstance():GetDatasByUid(_G.StorageID.Card,self.data.cardUid) or nil
    if isShow and cardData then
        self.go_table.aorimage_BoundBg:SetActiveE(1)
        self.go_table.aorimage_BoundBg.SpriteName = _G.CarvingRankEnum[_G.StorageManager:GetInstance():GetItemQuality(self.data.sid)]
        --local cfg = _G.ConfigManager.card_cfg[self.data.cardUid]
        local cfg = cardData.cfg
        if not cfg then
            return
        end
        local icon = cfg.Icon

        if _G.string.contains(icon, "/") then
            icon = _G.string.split2tbl(icon, "/")
            icon = icon[#icon]
        end
        self.go_table.aorimage_BoundIcon.SpriteName = icon
    else
        self.go_table.aorimage_BoundBg:SetActiveE(0)
    end
end

function ItemCarvingBase:ShowUp(isShow)
    self.go_table.obj_up:SetActiveE(isShow and 1 or 0)
end

function ItemCarvingBase:OnClickBtn(go, isOn)
    if self.click then
        self.click(self.clickParams)
    else
        local cfg = _G.ConfigManager.carving_cfg[self.sid]
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.RuneTipView.Name, self.data, cfg, true,nil,nil,false,true)
    end
end

---显示底框
function ItemCarvingBase:ShowFrame()
    self.go_table.aorimage_Color2.SpriteName = _G.CarvingRankEnum[_G.StorageManager:GetInstance():GetItemQuality(self.data.sid)]
    _G.GameUtil.SetActiveByCondition(self.go_table.aorimage_Color2, true)
end

return ItemCarvingBase
