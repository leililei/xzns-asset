---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2025/8/28 14:41
---
local base = require("BaseGameNet")
---@class ActiveSeverMergeNet : BaseGameNet	通信协议
local ActiveSeverMergeNet = _G.BaseClass("ActiveSeverMergeNet", base)

---接取任务
function ActiveSeverMergeNet:SendJieQuTask(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_accept
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---单条任务完成
---@param pbData table unit32 (sid = 1) 任务id unit32 (day=2) 任务所属天数
---@param callback function 完成回调 （task.s2c_task_finish：形式）
function ActiveSeverMergeNet:SendTaskOneFinish(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_finish
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---领取天数 的 全通奖励
---@param pbData number 领取的天数 (day = 1)
---@param callback function (public.s2c_common_ack 通用返回)
function ActiveSeverMergeNet:SendDayAllPassAward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_day_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---活动进度奖励
---@param pbData number 奖励 id(index = 1)
---@param callback funtion 完成回调（public.s2c_common_ack）
function ActiveSeverMergeNet:SendGoalsReward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_goals_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---活动签到
---@param pbData number 奖励 id(index = 1)
---@param callback funtion 完成回调（public.s2c_common_ack）
function ActiveSeverMergeNet:SendLoginAward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_login_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---兑换奖励
---@param pbData number 奖励 id(index = 1)
---@param callback funtion 完成回调（public.s2c_common_ack）
function ActiveSeverMergeNet:SendGoalsExchange(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_task_goals_exchange
    local port = base.Access(self, item, pbData, _G.handler(self, self.ActionGoalsCall))
    self.Call[port] = callback
end

---请求回调
---@param port number 流水号
---@param data table 返回PB数据
---@param error string 错误信息
function ActiveSeverMergeNet:ActionGoalsCall(port, data, error)
    if error then
        return
    end

    --TODO 写通用数据解析，没有需要做的记得删除TODO

    if self.Call[port] then
        self.Call[port](data)
        self.Call[port] = nil
    end
end

---@return ActiveSeverMergeNet ActiveSeverMergeNet
function ActiveSeverMergeNet:GetInstance()
    return self.super.super.GetInstance(self)
end

return ActiveSeverMergeNet