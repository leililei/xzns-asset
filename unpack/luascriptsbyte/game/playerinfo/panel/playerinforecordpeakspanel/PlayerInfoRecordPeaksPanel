-----------------------------------------------------------------------
-- C:.workspace.alpha_FT.Client.Assets.Resources.LuaScripts.Game.PlayerInfo.Panel.PlayerInfoRecordPeaksPanel	(修改路径必须修正此项!)
-- Created by sj
-- DateTime: 2023/06/09
-- Description: 玩家信息记录-巅峰演武（暂未开发）
-----------------------------------------------------------------------

local PlayerInfoNet = require("PlayerInfoNet"):GetInstance()
local base= require "PlayerInfoRecordBasePanel"

---@class PlayerInfoRecordPeaksPanel  :PlayerInfoRecordBasePanel
---@field public go_table PlayerInfoRecordView._obj_systemMaterial_GoTable
local PlayerInfoRecordPeaksPanel = _G.BaseClass("PlayerInfoRecordPeaksPanel", base)
_G.PlayerInfoRecordPeaksPanel = PlayerInfoRecordPeaksPanel

local CS = _G.CS
local typeof = _G.typeof
local GetAutoGoTable = _G.GetAutoGoTable
local handler = _G.handler

---类的构造函数
function PlayerInfoRecordPeaksPanel:__init(obj, uid, isBot)
    self.go_table = GetAutoGoTable(obj, handler(self, self.OnClickBtn))
    self.uid = uid
    self.isBot = isBot
end

---@private
function PlayerInfoRecordPeaksPanel:Show()
    --if not self.msg then
    --    local pbData = { role_uid = self.uid, page_id = 100501 }
    --    PlayerInfoNet:GetRoleFightRecord(pbData, handler(self, self.InitView))
    --end
end

---初始化
function PlayerInfoRecordPeaksPanel:InitView(msg)
end

---@private
function PlayerInfoRecordPeaksPanel:OnClickBtn(go, isOn)

end

---类的析构函数
function PlayerInfoRecordPeaksPanel:Dispose()
end

return PlayerInfoRecordPeaksPanel