-------------------------------------------------------------------------
-- D:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.PlayerInfo.Controller.PlayerNewCreateController	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2020/09/10
-- Description 此处填写描述
-----------------------------------------------------------------------

---@class PlayerNewCreateController : Singleton	中文描述
local PlayerNewCreateController = _G.BaseClass("PlayerNewCreateController", _G.Singleton)

---@return PlayerNewCreateController PlayerNewCreateController
function PlayerNewCreateController:GetInstance()
    return self.super.GetInstance(self)
end

---创建对象时,入口构造函数
function PlayerNewCreateController:__init()
end

---销毁对象时,析构函数
function PlayerNewCreateController:Dispose()
end

--region 对外接口
-----创建新角色
function PlayerNewCreateController:CreatePlayer()
    _G.WorldManager:GetInstance():EnterWorld(299, _G.handler(self, self.PlayTitle))
    _G.PlayerPrefsManager.SetTable(_G.LoginManager:GetInstance():GetCreatePlayerKey(), {last_room = -1}, true)
end
--endregion
--region流程
function PlayerNewCreateController:PlayTitle()
    _G.WorldItemManager:GetInstance():SetAllItemVisible(false)
    _G.UIManager:GetInstance():SwitchSceneCloseWindows()

    _G.UIManager:GetInstance():CloseWindow(_G.ConfigManager.ui_cfg.LoadingView.Name)
    self:PlayCameraAnimation()
end
--播放镜头动画
function PlayerNewCreateController:PlayCameraAnimation()
    -- local world = _G.WorldManager:GetInstance():GetCurWorld()
    -- _G.PoolsManager:GetInstance():Spawn(
    --     "CinemaClip/kaichang_cam/kaichang_cam",
    --     _G.ePoolType.Temporary,
    --     world.Transform,
    --     function(tran)
    -- _G.UIManager:GetInstance():OpenWindow(
    --     _G.ConfigManager.ui_cfg.ChapterTitleView.Name,
    --     _G.handler(
    --         self,
    --         function()
    --             -- world.WorldSetting.SceneCamera:SetActiveE(0)
    --             self:PlayStory()
    --         end
    --     )
    -- )

    --     end
    -- )

    self:PlayStory()
end
function PlayerNewCreateController:CreateRole()
    if _G.AppSetting.DEBUG then
        _G.Log("调用取名接口")
    end
    _G.PlayerModel:GetInstance():CreateRole(_G.handler(self, self.ExitWorld))
end
function PlayerNewCreateController:PlayStory()
    if _G.AppSetting.DEBUG then
        _G.Log("取名前对话")
    end
    --取名前对话
    -- _G.AutomaticPlayStory.PlayStoryByID(800000113, _G.handler(self, self.CreateRole))
    self:CreateRole()
end
function PlayerNewCreateController:ExitWorld()
    _G.LoginManager:GetInstance():StartLoginInfo()
end
--endregion

return PlayerNewCreateController
