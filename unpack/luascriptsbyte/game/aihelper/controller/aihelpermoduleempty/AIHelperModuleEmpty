local base = require("AIHelperModuleBase")

---@class AIHelperModuleEmpty: AIHelperModuleBase           @AI助手 - 生成Empty, 什么都不做什么都不显示
local AIHelperModuleEmpty = _G.BaseClass("AIHelperModuleEmpty", base)
function AIHelperModuleEmpty:ShowSettings(item)
    base.ShowSettings(self, item)
    self:SetSettingsType(item, 0)
end

return AIHelperModuleEmpty
