-------------------------------------------------------------------------
-- D:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.PopWindow.SociatyMobaTipsModel	(修改路径必须修正此项!)
-- Created by sj
-- DateTime: 2024/10/15
-- Description 此处填写描述
-----------------------------------------------------------------------

---@class SociatyMobaTipsModel : Singleton    中文描述
local SociatyMobaTipsModel = _G.BaseClass("SociatyMobaTipsModel", _G.Singleton)

local CS = _G.CS
local typeof = _G.typeof

---@return SociatyMobaTipsModel SociatyMobaTipsModel
function SociatyMobaTipsModel:GetInstance()
    return self.super.GetInstance(self)
end

---创建对象时,入口构造函数
function SociatyMobaTipsModel:__init()
end

---销毁对象时,析构函数
function SociatyMobaTipsModel:Dispose()
end

--region对外接口
function SociatyMobaTipsModel:Show(content, pos)
    if self.view then
        self.view:Add(content)
    else
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.SociatyMobaTipsView.Name, content, pos)
    end
end

function SociatyMobaTipsModel:SetView(view)
    self.view = view
end

function SociatyMobaTipsModel:CloseView()
    _G.UIManager:GetInstance():CloseWindow(_G.ConfigManager.ui_cfg.SociatyMobaTipsView.Name)
end

return SociatyMobaTipsModel
--endregion
