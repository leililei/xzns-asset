-- Game.HUD.View.HUDView	(修改路径必须修正此项!)
-- Created by TangHuan
-- DateTime: 2020/03/23
-- 生命周期Start, OnDisable, OnEnable, OnDispose 可根据情况自行添加

--- 所有HUD层显示的东西都可以做在这一个窗口上
--- Note:
--- 此窗口默认位于SceneLayer层

---@class HUDView : UIBaseView	HUD显示窗口
---@field public go_table HUDView_GoTable GoTable
local HUDView = _G.BaseClass("HUDView", _G.UIBaseView)

function HUDView:Awake()
end

---界面的入口函数
function HUDView:OnCreate()
end

---响应HUD界面关闭
function HUDView:Dispose()
    --("HUDView属于常驻窗口，不应该被关闭!")
end

---@type HUDView
return HUDView
