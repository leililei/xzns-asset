-----------------------------------------------------------------------
-- C:.alpha_JT.Client.Assets.Resources.LuaScripts.Game.SociatyMaze.View.SociatyMazeePassAwardTip	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2024/12/17
-- 生命周期Start, OnDisable, OnEnable, OnDispose 可根据情况自行添加
-----------------------------------------------------------------------

---@class SociatyMazeePassAwardTip : UIBaseView	中文描述
---@field public go_table SociatyMazeePassAwardTip_GoTable GoTable
local SociatyMazeePassAwardTip = _G.BaseClass("SociatyMazeePassAwardTip", _G.UIBaseView)

function SociatyMazeePassAwardTip:Awake()
    _G.AddViewBtnClick(self.go_table.gameObject, _G.handler(self, self.OnClickBtn))
end

---界面的入口函数
function SociatyMazeePassAwardTip:OnCreate(callback)
    self.callback = callback
    _G.DelayDo(1.5, function()
        if self:IsNil() then
            return
        end

        self:Close()
        if callback then
           self.callback() 
        end
    end)
end

---界面的销毁函数
function SociatyMazeePassAwardTip:OnDestroy()
end

---按钮点击事件在这里派发
function SociatyMazeePassAwardTip:OnClickBtn(go)
end

return SociatyMazeePassAwardTip
