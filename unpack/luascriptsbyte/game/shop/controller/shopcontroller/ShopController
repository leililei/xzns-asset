--------------------------------------------------------------------------
-- E:.Game.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.Shop.Controller.ShopController	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/01/11
-- Description 此处填写描述
-----------------------------------------------------------------------

---@class ShopController : Singleton	中文描述
local ShopController = _G.BaseClass("ShopController", _G.Singleton)
_G.ShopController = ShopController

---@return ShopController ShopController
function ShopController:GetInstance()
    return self.super.GetInstance(self)
end

---创建对象时,入口构造函数
function ShopController:__init()
end

---销毁对象时,析构函数
function ShopController:Dispose()
end

--region  查看功能是否 开启
---@param functionId number 功能id
function ShopController:CheckFuntionOpenShop(functionId)
    local flag = false
    flag = _G.FunctionManager:GetInstance():CheckFunctionIsUnlock(functionId)
    return flag
end
--endregin
--region 管理界面
---打开界面
---@param viewID number
function ShopController:OpenUIWindow(viewID)
    if viewID == _G.ShopViewEnum.hunyu then
        ---魂玉
        ---@type params_MallView
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.MallView.Name, _G.MallViewPage.recharge)
    elseif viewID == _G.ShopViewEnum.yueka then
        -- elseif viewID == _G.ShopViewEnum.tehui then
        --     ---特惠
        --     _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.ShopPreferenceView.Name)
        ---月卡

        ---@type params_MallView
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.MallView.Name, _G.MallViewPage.monthly)
    elseif viewID == _G.ShopViewEnum.huojia then
        ---弃用
        -- elseif viewID == _G.ShopViewEnum.zahuo then
        --     ---杂货
        --     if _G.FunctionManager:GetInstance():CheckFunctionIsUnlock(25) then
        --         _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.MallView.Name, _G.MallViewPage.token)
        --     else
        --         local cfg = _G.ConfigManager.afk_cfg[_G.ConfigManager.function_open_cfg[25].condition_params]
        --         _G.PopWindowManager:GetInstance():ShowAllTips(
        --             110119,
        --             _G.LangUtil.GetLangPackValue(91300009910, cfg.chapter, cfg.section)
        --         )
        --     end
        ---货架
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.MallView.Name, _G.MallViewPage.gift)
    else
        _G.UIManager:GetInstance():OpenWindow(_G.ConfigManager.ui_cfg.MallView.Name)
    end
end
--endregin
return ShopController
