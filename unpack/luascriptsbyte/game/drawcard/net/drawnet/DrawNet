-----------------------------------------------------------------------
-- E:.01.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.DrawCard.Net.DrawNet	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2020/11/20
-- Description 通信协议
-----------------------------------------------------------------------

---@class DrawNet : BaseGameNet	通信协议
local DrawNet = _G.BaseClass("DrawNet", require("BaseGameNet"))
local base = require("BaseGameNet")

---@return DrawNet DrawNet
function DrawNet:GetInstance()
    return self.super.super.GetInstance(self)
end

-- ---获取奖励请求
-- ---@param pbData table PB数据
-- ---@param callback function 完成回调
-- function DrawNet:GetAward(callback)
--     local item = _G.ConfigManager.net_map.game_lucky_draw_port_get_total_award
--     local port = base.Access(self, item, self:GetEmptyPB(), _G.handler(self, self.CallBack))
--     self.Call[port] = callback
-- end

---抽卡请求
---@param pbData table PB数据
---@param callback function 完成回调
function DrawNet:DrawCard(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_execute
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end
---奖励抽卡请求
---@param pbData table PB数据
---@param callback function 完成回调
function DrawNet:ChoKaDrawCard(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_choose_element_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end
---解锁五行池请求
---@param pbData table PB数据
---@param callback function 完成回调
function DrawNet:UnlockPool(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_unlock_pool
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end

---设置心愿单
function DrawNet:SetWishList(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_set_wish_card
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end

---获取抽奖信息
---@param pbData table PB数据
---@param callback function 完成回调
function DrawNet:GetDrawCardInfo(callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_get_info
    local port = base.Access(self, item, self:GetEmptyPB(), _G.handler(self, self.CallBack))
    self.Call[port] = callback
end
---设置降神卡
function DrawNet:SetGodCard(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_set_befall_card
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end

function DrawNet:DrawGodCard(pbData, callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_port_befall_execute
    local port = base.Access(self, item, pbData, _G.handler(self, self.CallBack))
    self.Call[port] = callback
end

--获取抽卡累计奖励
---@param callback function 完成回调
function DrawNet:GetDrawExAward(callback)
    local item = _G.ConfigManager.net_map.game_lucky_draw_get_total_award
    local port = base.Access(self, item, self:GetEmptyPB(), _G.handler(self, self.CallBack))
    self.Call[port] = callback
end

---请求回调
---@param port number 流水号
---@param data table 返回PB数据
---@param error string 错误信息
function DrawNet:CallBack(port, data, error)
    if error then
        return
    end

    --TODO 写通用数据解析，没有需要做的记得删除TODO

    if self.Call[port] then
        self.Call[port](data)
        self.Call[port] = nil
    end
end

---@type DrawNet
return DrawNet
