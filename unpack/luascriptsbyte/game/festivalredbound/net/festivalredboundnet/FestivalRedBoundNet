-----------------------------------------------------------------------
-- C:.workspace.alpha_FT.Client.Assets.Resources.LuaScripts.Game.HeroicIcon.Net.FestivalRedBoundNet	(修改路径必须修正此项!)
-- Created by 
-- DateTime: 
-- Description 英雄圣像
-----------------------------------------------------------------------

local base = require("BaseGameNet")

---@class FestivalRedBoundNet : BaseGameNet    通信协议
local FestivalRedBoundNet = _G.BaseClass("FestivalRedBoundNet", base)
---@return FestivalRedBoundNet FestivalRedBoundNet
function FestivalRedBoundNet:GetInstance()
    return base.GetInstance(self)
end

---开始游戏
function FestivalRedBoundNet:StartGame(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_start_game
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end

---游戏完成
function FestivalRedBoundNet:FinishGame(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_finish_game
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end

---游戏扫荡
function FestivalRedBoundNet:Sweep(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_sweep
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end

---领取达标奖励
function FestivalRedBoundNet:GetQualifyAward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_qualify_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end

---领取战令累计奖励
function FestivalRedBoundNet:GetExtraAward(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_extra_award
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end

---领取门票奖励
function FestivalRedBoundNet:GetTicket(pbData, callback)
    local item = _G.ConfigManager.net_map.game_activity_mini_game_ticket
    local port = base.Access(self, item, pbData, _G.handler(self, self.Callback))
    self.Call[port] = callback
end




---请求回调
---@param port number 流水号
---@param data table 返回PB数据
---@param error string 错误信息
function FestivalRedBoundNet:Callback(port, data, error)
    if error then
        return
    end
    if self.Call[port] then
        self.Call[port](data)
        self.Call[port] = nil
    end
end

return FestivalRedBoundNet