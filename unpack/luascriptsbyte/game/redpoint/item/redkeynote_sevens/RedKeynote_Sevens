--------------------------------------------------------------------------
-- E:.Game.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedKeynote_Sevens	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2021/12/30
-- Description: 此处填写描述
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedKeynote_Sevens	中文描述  七日目标(成王之路) daybutton 页签上红点
local RedKeynote_Sevens = _G.BaseClass("RedKeynote_Sevens", base)

--region子类重写
---获取所有参数
---@return table 参数列表
function RedKeynote_Sevens:GetAllParams()
    -- if self.allActionDay then
    --     return self.allActionDay
    -- end
    self.allActionDay = {1, 2, 3, 4, 5, 6, 7}
    -- --期数
    -- local qishu, bol = require "ActionKeynoteModel":GetInstance():GetCurrentQiShu()
    -- if bol then 
    --     for i = 1, 7 do
    --         local param = qishu * 100
    --         param = param + i
    --         table.insert(self.allActionDay, param)
    --     end
    -- end
    return self.allActionDay
end

---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedKeynote_Sevens:CheckParams(param)
    return require("ActionKeynoteModel"):GetInstance():GetRedPointActionDayNums(param)
end

function RedKeynote_Sevens:ParseParamForParent(param)
    --local qishu = math.modf(param / 100)
    return { param }
end

---@param param number 期数的id
function RedKeynote_Sevens:GetRedPointNumByParentParam(param)
    -- local num = 0
    -- param = param * 100
    -- for i = 1, 7 do
    --     local pointnum = self:GetRedPointNum(param + i)
    --     if pointnum then
    --         num = num + pointnum
    --     end
    -- end
    return self:GetRedPointNum(param)
end

--endregion

return RedKeynote_Sevens
