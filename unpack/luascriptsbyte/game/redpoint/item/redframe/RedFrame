--------------------------------------------------------------------------
-- D:.Game.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedFrame	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/06/26
-- Description: 此处填写描述
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedFrame	头像框红点
local RedFrame = _G.BaseClass("RedFrame", base)

--region 检测逻辑
--当玩家有新解锁的头像/头像框时，对应头像和头像框增加红点 （显示红点）
--玩家点击对应带红点的头像/头像框时，红点消失 （点击消失红点）
--endregion
--region子类重写
---获取所有参数
---@return table 参数列表
function RedFrame:GetAllParams()
    if self.allFrameIDs then
        return self.allFrameIDs
    end
    local framcfg = _G.ConfigManager.player_frame_cfg
    local table = _G.table
    self.allFrameIDs = {}
    for key, value in pairs(framcfg) do
        table.insert(self.allFrameIDs, value.ID)
    end
    return self.allFrameIDs
end
---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedFrame:CheckParams(param)
    local sid = param
    --头像框默认第一个 不显示红点
    if sid == 100 then
        return 0
    end
    --判断是不已经解锁
    if _G.PlayerModel:GetInstance():IsUnlockFrame(sid) then
    else
        return 0
    end
    -- local cfg = _G.ConfigManager.player_frame_cfg[sid]
    -- local type = cfg.unlock_type
    -- local flag = false
    -- if type == 1 then
    --     if cfg.unlock_params then
    --         for i = 1, #cfg.unlock_params do
    --             local constain = require "IllustrationModel":GetInstance():HunjiangJieSuo(cfg.unlock_params[i])
    --             if constain then
    --                 flag = true
    --             else
    --                 return 0
    --             end
    --         end
    --     else
    --         return 0
    --     end
    -- elseif type == 0 then
    --     return 0
    -- end
    -- if not flag then
    --     return 0
    -- end
    -- --判断本地有没有数据 1 == 解锁 0 == 已经点过
    local key = tostring(_G.PlayerModel:GetInstance().role_uid) .. "frame" .. sid
    if _G.CS.WechatGameCore.PlayerPrefs.HasKey(key) then
        if _G.CS.WechatGameCore.PlayerPrefs.GetInt(key) == 0 then
            return 0
        end
    end
    return 1
end
-- ---解析参数返回给父节点
-- ---@param param number 子节点参数
-- ---@return number 父节点参数
-- function RedFrame:ParseParamForParent(param)
--     local frameid = math.modf(param / 10)
--     return frameid
-- end
-- ---根据父节点传过来的参数获取红点数量
-- ---@param param number 父节点参数
-- ---@return number 红点数量
-- function RedFrame:GetRedPointNumByParentParam(param)
--     local num = 0
--     param = param * 10
--     local point = self:GetRedPointNum(param + 1)
--     if point then
--         num = num + point
--     end
--     return num
-- end
--endregion

return RedFrame
