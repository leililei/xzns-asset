-----------------------------------------------------------------------
-- D:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointItemArenaRankAwardSenior	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/05/26
-- Description: 高阶竞技场段位奖励
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedPointItemArenaRankAwardSenior	中文描述
local RedPointItemArenaRankAwardSenior = _G.BaseClass("RedPointItemArenaRankAwardSenior", base)

--region子类重写
---获取所有参数
---@return table 参数列表
function RedPointItemArenaRankAwardSenior:GetAllParams()
    --ZHJ-31282屏蔽
    -- if self.allParams then
    --     return self.allParams
    -- end
    -- self.allParams = {false, false, false, false, false, false, false, false}
    -- local index = 1
    -- for _, value in pairs(_G.ConfigManager.arena_senior_rank_cfg) do
    --     self.allParams[index] = value.id
    --     index = index + 1
    -- end
    -- return self.allParams
    return {}
end
---检测全部
---@return number 红点数量
function RedPointItemArenaRankAwardSenior:CheckAll()
    --ZHJ-31282屏蔽
    -- local ArenaManager = require("ArenaManager")
    -- if not ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.senior):CheckSeasonIsOpen() then
    --     return 0
    -- end
    -- local arenaClass = require("ArenaManager"):GetInstance():GetArenaClass(2)
    -- local cfgs = _G.ConfigManager.arena_senior_rank_cfg
    -- for i = 1, #self.allParams do
    --     if
    --         not arenaClass:GetRankAwardState(self.allParams[i]) and
    --             arenaClass:CheckRankAwardTo(cfgs[self.allParams[i]].condition_level)
    --      then
    --         return 1
    --     end
    -- end
    -- return 0
    return 0
end
---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointItemArenaRankAwardSenior:CheckParams(param)
    --ZHJ-31282屏蔽
    -- local ArenaManager = require("ArenaManager")
    -- if not ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.senior):CheckSeasonIsOpen() then
    --     return 0
    -- end
    -- local arenaClass = require("ArenaManager"):GetInstance():GetArenaClass(2)
    -- local cfgs = _G.ConfigManager.arena_senior_rank_cfg
    -- if not arenaClass:GetRankAwardState(param) and arenaClass:CheckRankAwardTo(cfgs[param].condition_level) then
    --     return 1
    -- end
    -- return 0
    return 0
end
--endregion

return RedPointItemArenaRankAwardSenior
