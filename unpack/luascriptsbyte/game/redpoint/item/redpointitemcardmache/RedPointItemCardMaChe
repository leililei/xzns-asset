-----------------------------------------------------------------------
-- D:.Project.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointItemCardMaChe	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2021/12/30
-- Description:
---就解锁显示一次 之后都不显示 除非换手机

-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedPointItemCardMaChe	返魂马车 红点检测
local RedPointItemCardMaChe = _G.BaseClass("RedPointItemCardMaChe", base)

function RedPointItemCardMaChe:GetAllParams()
    return {0}
end
---检测全部
---@return number 红点数量
function RedPointItemCardMaChe:CheckAll()
    local Red_CardMaChe_num = 0
    -- if _G.PlayerPrefsManager.GetInt("Red_CardMache") == 1 then
    --     --- 点开过就不显示
    --     Red_CardMaChe_num = 0
    -- if _G.FunctionManager:GetInstance():CheckFunctionIsUnlock(43) then
    --     --- 解锁了就显示
    --     Red_CardMaChe_num = 1
    -- end
    return Red_CardMaChe_num
end
---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointItemCardMaChe:CheckParams(param)
    return self:CheckAll()
end
--endregion

return RedPointItemCardMaChe
