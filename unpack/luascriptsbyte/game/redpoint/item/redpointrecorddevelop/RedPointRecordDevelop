---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2025/11/8 16:07
---

local base = require "RedPointItemBase"
---@class RedPointRecordDevelop	中文描述
local RedPointRecordDevelop = BaseClass("RedPointRecordDevelop",base)

---获取所有参数
---@return table 参数列表
function RedPointRecordDevelop:GetAllParams()
    if self.allLingRankIDs then
        return self.allLingRankIDs
    end
    local table = _G.table
    self.allLingRankIDs = {}
    table.insert(self.allLingRankIDs, 1)
    table.insert(self.allLingRankIDs, 2)
    table.insert(self.allLingRankIDs, 3)
    table.insert(self.allLingRankIDs, 4)
    table.insert(self.allLingRankIDs, 6)
    return self.allLingRankIDs
end

---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointRecordDevelop:CheckParams(param)
    local model = require("RecordRankModel"):GetInstance()
    --检测是否开启
    if not _G.FunctionManager:GetInstance():CheckFunctionIsUnlock(48) then
        return 0
    end
    if param == 6 and not model:CheckGodShow() then
        return 0
    end

    local rankRedNum = self:GetRankRedNum(param)
    local rewardRedNum = self:GetRewardRedNum(param)
    if rankRedNum ~= 0 or rewardRedNum ~= 0 then
        return 1
    end

    return 0
end

function RedPointRecordDevelop:GetRewardRedNum(param)
    local model = require("RecordRankModel"):GetInstance()
    if model:HasAwardReceive(param) then
        return 1
    end
    return 0
end

function RedPointRecordDevelop:GetRankRedNum(param)
    local model = require("RecordRankModel"):GetInstance()
    --检查是否有上榜人数
    if model:GetRankPlayerCount(param) <= 0 then
        return 0
    end
    if model:HasLinkAward() == false then
        return 0
    end
    return 1
end

---解析参数返回给父节点
---@param param number 子节点参数
---@return number 父节点参数
function RedPointRecordDevelop:ParseParamForParent(param)
    return {param}
end



return RedPointRecordDevelop