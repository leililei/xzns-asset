-----------------------------------------------------------------------
-- G:.zhenhunjie.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointItemArenaElementLeague	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/09/27
-- Description: 五行联赛段位奖励红点
-----------------------------------------------------------------------
local ArenaManager = require("ArenaManager")
local base = require "RedPointItemBase"
---@class RedPointItemArenaElementLeague	中文描述
local RedPointItemArenaElementLeague = _G.BaseClass("RedPointItemArenaElementLeague", base)

--region子类重写
---额外的功能开放检测
---@return boolean 是否开放
function RedPointItemArenaElementLeague:CheckFunctionOpenExtra()
    --ZHJ-31282屏蔽
    -- return ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element):GetCurArenaState() ==
    --     ArenaManager.State.open
    return false
end
---获取所有参数
---@return table 参数列表
function RedPointItemArenaElementLeague:GetAllParams()
    --ZHJ-31282屏蔽
    -- if self.allRankIDs then
    --     return self.allRankIDs
    -- end
    -- self.allRankIDs = _G.table.keys(_G.ConfigManager.arena_league_elem_cfg)
    -- _G.table.sort(self.allRankIDs)
    -- return self.allRankIDs
    return {}
end
---检测全部
---@return number 红点数量
function RedPointItemArenaElementLeague:CheckAll()
    --ZHJ-31282屏蔽
    -- local num = 0
    -- local cfgs = _G.ConfigManager.arena_league_elem_cfg
    -- local cfg
    -- local emptyTableCheck = _G.table.isEmptyOrNil
    -- local arenaClass = ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element)
    -- for i = 1, #self.allRankIDs do
    --     cfg = cfgs[self.allRankIDs[i]]
    --     if not emptyTableCheck(cfg.reward_level) then
    --         if not arenaClass:GetRankAwardState(self.allRankIDs[i]) then
    --             if cfg.rank_con <= 0 then
    --                 if arenaClass:GetMaxScore() >= cfg.min_score then
    --                     num = num + 1
    --                 end
    --             else
    --                 if arenaClass:GetMaxScore() >= cfg.min_score and self:GetMaxRank(1) <= cfg.rank_con then
    --                     num = num + 1
    --                 end
    --             end
    --         end
    --     end
    -- end
    -- return num
    return 0
end
---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointItemArenaElementLeague:CheckParams(param)
    --ZHJ-31282屏蔽
    -- local cfg = _G.ConfigManager.arena_league_elem_cfg[param]
    -- if _G.table.isEmptyOrNil(cfg.reward_level) then
    --     return 0
    -- end
    -- local arenaClass = ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element)
    -- if not arenaClass:GetRankAwardState(param) then
    --     if cfg.rank_con <= 0 then
    --         return arenaClass:GetMaxScore() >= cfg.min_score and 1 or 0
    --     else
    --         return (arenaClass:GetMaxScore() >= cfg.min_score and self:GetMaxRank(1) <= cfg.rank_con) and 1 or 0
    --     end
    -- end
    -- return 0
    return 0
end
--endregion

return RedPointItemArenaElementLeague
