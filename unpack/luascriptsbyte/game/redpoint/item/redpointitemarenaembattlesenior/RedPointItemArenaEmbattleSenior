-----------------------------------------------------------------------
-- D:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointItemArenaEmbattleSenior	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/05/27
-- Description: 此处填写描述
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedPointItemArenaEmbattleSenior	中文描述
local RedPointItemArenaEmbattleSenior = _G.BaseClass("RedPointItemArenaEmbattleSenior", base)

--region子类重写
---检测全部
---@return number 红点数量
function RedPointItemArenaEmbattleSenior:CheckAll()
    --ZHJ-31282屏蔽
    -- local ArenaManager = require("ArenaManager")
    -- if not ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.senior):CheckSeasonIsOpen() then
    --     return 0
    -- end
    -- local EmbattleType = _G.EmbattleType
    -- local EmbattleModelIns = require("EmbattleModel"):GetInstance()
    -- local ets = {EmbattleType.arenaSeniorDefence1, EmbattleType.arenaSeniorDefence2, EmbattleType.arenaSeniorDefence3}
    -- for i = 1, #ets do
    --     local data = EmbattleModelIns:GetEmbattleData(ets[i])
    --     for j = 1, #data do
    --         if data[j] == 0 then
    --             return 1
    --         end
    --     end
    -- end
    -- return 0
    return 0

    ------
    --local ets = {EmbattleType.arenaSeniorAttack1, EmbattleType.arenaSeniorAttack2, EmbattleType.arenaSeniorAttack3}
    --for i = 1, #ets do
    --    local data = EmbattleModelIns:GetEmbattleData(ets[i])
    --    for j = 1, #data do
    --        if data[j] > 0 then
    --            break
    --        end
    --        if j == #data then
    --            return 1
    --        end
    --    end
    --end
    --return 0
end
--endregion

return RedPointItemArenaEmbattleSenior
