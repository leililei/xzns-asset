-----------------------------------------------------------------------
-- E:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointHellGuard	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/08/06
-- Description: 此处填写描述
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedPointHellGuard	中文描述
local RedPointHellGuard = _G.BaseClass("RedPointHellGuard", base)

--region子类重写
---获取所有参数
---@return table 参数列表
function RedPointHellGuard:GetAllParams()
    return {0, 1, 2}
end

---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointHellGuard:CheckParams(param)
    -- if self:CheckShow() then
    return require "HellGuardModel":GetInstance():CheckRedPoint(param) and 1 or 0
    --  else
    --     return 0
    --end
end
--endregion
function RedPointHellGuard:CheckShow()
    local FunctionManagerIns = _G.FunctionManager:GetInstance()
    if FunctionManagerIns:CheckFunctionIsUnlock(4) or FunctionManagerIns:CheckFunctionIsUnlock(80) then
        return true
    end
    return false
end
return RedPointHellGuard
