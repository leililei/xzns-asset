-----------------------------------------------------------------------
-- E:.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointCardFetter_Page	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2023/02/17
-- Description: 此处填写描述
-----------------------------------------------------------------------

local base = require "RedPointItemBase"
---@class RedPointCardFetter_Page	中文描述
local RedPointCardFetter_Page = _G.BaseClass("RedPointCardFetter_Page", base)

--region子类重写
---获取所有参数
---@return table 参数列表
function RedPointCardFetter_Page:GetAllParams()
    -- local cards = _G.StorageManager:GetInstance():GetDataByID(_G.StorageID.Card, _G.CardType.Normal)
    -- local table = _G.table
    -- local part_uid = 0
    -- self.cardUid = {}
    -- for key, value in pairs(cards) do
    --     part_uid = value.uid
    --     table.insert(self.cardUid, part_uid)
    -- end
   -- return self.cardUid
   return {}
end
function RedPointCardFetter_Page:ParseParamForParent(param)
    return {param}
end

-- ---检测单个红点
-- ---@param param number 参数
-- ---@return number 红点数量
-- function RedPointCardFetter_Page:CheckParams(param)
--     ---@type CardData
--     local data = _G.StorageManager:GetInstance():GetDatasByUid(_G.StorageID.Card, param)
--     return require("CardFetterManager"):GetInstance():CheckHasActive(data.sid) and 1 or 0
-- end

-- ---@param param number 卡牌的 uid
-- function RedPointCardFetter_Page:GetRedPointNumByParentParam(param)
--     local num = 0
--     local point = self:GetRedPointNum(param)
--     if point then
--         num = num + point
--     end
--     return num
-- end
--endregion

return RedPointCardFetter_Page
