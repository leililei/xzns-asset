---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2024/4/11 12:08
---

local base = require "RedPointItemBase"
---@class RedPointSevenGift	中文描述
local RedPointSevenGift = _G.BaseClass("RedPointSevenGift", base)

--region子类重写
---获取所有参数
---@return table 参数列表
-- function RedPointSevenGift:GetAllParams()
--     local model = require "SevenGiftModel":GetInstance()
--     local openGift = model:GetOpenGift()
--     local T = {}
--     for k, v in ipairs(openGift) do 
--         table.insert(T, v.id)
--     end
--     return T
-- end

-- ---检测单个红点
-- ---@param param number 参数
-- ---@return number 红点数量
-- function RedPointSevenGift:CheckParams(param)
--     local value = _G.PlayerPrefsManager.GetInt("SevenGiftModel_" .. param, 0)
--     local curTime = _G.TimeUtil.GetServerUnixTime()
--     if TimeUtil.GetDayBySix(value) ~= TimeUtil.GetDayBySix(curTime) then
--         return 1
--     end

--     return 0
-- end

---检测全部
---@return number 红点数量
function RedPointSevenGift:CheckAll()
    
    local curTime = _G.TimeUtil.GetServerUnixTime()
    local value = _G.PlayerPrefsManager.GetInt("SevenGiftModel", curTime - 86400)
    if TimeUtil.GetDayBySix(value) ~= TimeUtil.GetDayBySix(curTime) then
        return 1
    end

    return 0
end


return RedPointSevenGift