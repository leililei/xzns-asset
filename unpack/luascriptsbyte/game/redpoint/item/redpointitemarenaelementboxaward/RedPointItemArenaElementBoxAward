-----------------------------------------------------------------------
-- G:.zhenhunjie.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.RedPoint.Item.RedPointItemArenaElementBoxAward	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/09/27
-- Description: 五行联赛胜点奖励红点
-----------------------------------------------------------------------
local ArenaManager = require "ArenaManager"
local base = require "RedPointItemBase"
---@class RedPointItemArenaElementBoxAward	中文描述
local RedPointItemArenaElementBoxAward = _G.BaseClass("RedPointItemArenaElementBoxAward", base)

--region子类重写
---额外的功能开放检测
---@return boolean 是否开放
function RedPointItemArenaElementBoxAward:CheckFunctionOpenExtra()
    --ZHJ-31282屏蔽
    -- return ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element):GetCurArenaState() ==
    --     ArenaManager.State.open
    return false
end
---获取所有参数
---@return table 参数列表
function RedPointItemArenaElementBoxAward:GetAllParams()
    --ZHJ-31282屏蔽
    -- if self.allBoxPoints then
    --     return self.allBoxPoints
    -- end
    -- local cfg = _G.ConfigManager.arena_info_cfg[ArenaManager.arenaType.element]
    -- self.allBoxPoints = {cfg.points_con1, cfg.points_con2, cfg.points_con3}
    -- return self.allBoxPoints
    return {}
end
---检测全部
---@return number 红点数量
function RedPointItemArenaElementBoxAward:CheckAll()
    --ZHJ-31282屏蔽
    -- local arenaClass = ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element)
    -- local winTimes = arenaClass:GetWinNum()
    -- local num = 0
    -- for i = 1, #self.allBoxPoints do
    --     if winTimes >= self.allBoxPoints[i] and not arenaClass:GetWinBoxState(self.allBoxPoints[i]) then
    --         num = num + 1
    --     end
    -- end
    -- return num
    return 0
end
---检测单个红点
---@param param number 参数
---@return number 红点数量
function RedPointItemArenaElementBoxAward:CheckParams(param)
    --ZHJ-31282屏蔽
    -- local arenaClass = ArenaManager:GetInstance():GetArenaClass(ArenaManager.arenaType.element)
    -- local winTimes = arenaClass:GetWinNum() or 0
    -- for _, v in pairs(self.allBoxPoints) do
    --     if winTimes >= v and not arenaClass:GetWinBoxState(v) then
    --         return 1
    --     end
    -- end
    -- return 0
    return 0
end
--endregion

return RedPointItemArenaElementBoxAward
