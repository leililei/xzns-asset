---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2025/11/10 16:29
---

local base = require "RedPointItemBase"
---@class RecordRankChallengeTag : RedPointItemBase
local RecordRankChallengeTag = BaseClass("RecordRankChallengeTag",base)


function RecordRankChallengeTag:Check(param, manager)
    return 0
end


function RecordRankChallengeTag:GetRewardNum(param)
    local model = require("RecordRankModel"):GetInstance()

    local lstCfg = model:GetBookCfg(param)
    if not lstCfg then
        return 0
    end
    if param == 6 and not require("RecordRankModel"):GetInstance():CheckGodShow() then
        return 0
    end
    for _, cfg in pairs(lstCfg) do
        --检查是否有可领取的奖励
        if model:GetBookRecord(cfg.id) and model:CanGetBookAward(cfg.id) then
            return 1
        end
    end
    return 0
end


return RecordRankChallengeTag