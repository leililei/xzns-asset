---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2025/7/21 10:28
---

local RankUtils = require("RankUtils")
local AllianceRewardItem = require("AllianceRewardItem")

---@class ArenaGlobalPeakRewardItem : PersonalRankRewardItem 联盟排行奖励item
---@field go_table AllianceRewardsBaseView._obj_ActiveDisRewardItem_GoTable
---@field super PersonalRankRewardItem
local ArenaGlobalPeakRewardItem = _G.BaseClass("ArenaGlobalPeakRewardItem", AllianceRewardItem)

function ArenaGlobalPeakRewardItem:RefreshItem(data, index)
    self.rewardList = self:GetShowCfgRewardList(data)

    self.data = data
    self.index = index

    RankUtils.SetRankItemBgIcon(self.go_table.aorimage_bg,index)
    if index <= 3 then
        --RankUtils.SetObj_SpecialRankNode(self.SpecialRankNode,index)
        self.go_table.aortext_rank.text = string.format("%d-%d",data.min_rank,data.max_rank)
    end
    if self.data.min_rank == self.data.max_rank then
        self.go_table.aortext_rank.text = _G.GetLangPackValue(90022100031, data.min_rank)
    elseif data.max_rank > 999 then
        self.go_table.aortext_rank.text = _G.GetLangPackValue(90033404108)
    else
        self.go_table.aortext_rank.text = _G.GetLangPackValue(90022100071, data.min_rank, data.max_rank)
    end
    _G.GameUtil.ReBuildRTF(self.go_table.obj_nameNode)

    self:RefreshRewardNode()

    self.isLoadTitle = false
    self:LoadTitle()
end


return ArenaGlobalPeakRewardItem
