-----------------------------------------------------------------------
-- E:.zhenhunjie_client.zhj_beta_1.0.0_Jt_H5.Client.Assets.Resources.LuaScripts.Game.View.BagRechargeAccumulateView	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2025/01/13
-- 生命周期Start, OnDisable, OnEnable, OnDispose 可根据情况自行添加
-----------------------------------------------------------------------
local baseView = require("BagPropInfoTipView")
---@class BagRechargeAccumulateView : BagPropInfoTipView	中文描述
---@field public go_table BagRechargeAccumulateView_GoTable GoTable
local BagRechargeAccumulateView = _G.BaseClass("BagRechargeAccumulateView", baseView)

function BagRechargeAccumulateView:OnUICreate()
    local path = "UIH5/Common/CommonTipView/View/$_obj_RechargeAccumulate"
    _G.PoolsManager:GetInstance():Spawn(
        path,
        _G.ePoolType.Temporary,
        self.go_table.obj_tipView,
        function(trans)
            trans:RequireComponentE(_G.typeof(_G.CS.AorGraphicRaycaster))
            local view =
                self:AddComponent(trans.gameObject, require("RechargeAccumulateTipView").New(trans, self, true))
            view:Init(self.itemData)
            self.view = view
        end
    )
end

function BagRechargeAccumulateView:RefreshView()
    
end

function BagRechargeAccumulateView:CloseTip()
    self:OnCloseView()   
end

return BagRechargeAccumulateView
