-- D:.Project.zhenhunjie-s.client.trunk.alpha.Client.Assets.Resources.LuaScripts.Game.Story.Action.ShowTipsStoryAction	(修改路径必须修正此项!)
-- Created by 此处填写你的名字
-- DateTime: 2022/03/31
-- Description: 此处填写描述

---@class ShowTipsStoryAction : BaseStoryAction @显示tips
---@field ActionData ShowTipsActionData @行为数据
local ShowTipsStoryAction = _G.BaseClass("ShowTipsStoryAction", _G.BaseStoryAction)

local base = _G.BaseStoryAction

---ShowTipsStoryAction的构造函数
---@param storyactionid number @行为ID
function ShowTipsStoryAction:__init(storyactionid)
end

---剧情动作执行
function ShowTipsStoryAction:Excute()
    --TODO:摄像机移动代码待添加
    if self.ActionData.WaitExecuteComplete == false then
        self:DOFun()
    end
    base.Excute(self)
end
function ShowTipsStoryAction:DoWaitExecuteAction()
    self:DOFun()
end

---行为
function ShowTipsStoryAction:DOFun()
    _G.PopWindowManager:ShowAllTips(1001, _G.LangUtil.GetLangPackValue(self.ActionData.LangId))
    self:Complete()
end
---跳过剧情行为必须做的部分
function ShowTipsStoryAction:DoMustDoForSkip()
end

function ShowTipsStoryAction:Complete()
    if self.ActionData.WaitExecuteComplete == true then
        self:ActionExecuteCompleteCB()
    end
end

---退出
function ShowTipsStoryAction:Exit()
    base.Exit(self)
end
---@type ShowTipsStoryAction @行为
return ShowTipsStoryAction
