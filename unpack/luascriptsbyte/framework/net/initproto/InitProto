--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by wyx.
--- DateTime: 2019/11/8 10:56
--- 加载Proto文件,初始化发送和接收推送table

_G.pb = require "pb"

--local protoc = require "protoc"
local CS = _G.CS

local dic = _G.CS.Main.Inst.ProtoDataCache

for _, value in pairs(dic) do
    _G.pb.load(value)
end

-- 消息模拟工具需要等待PB文件加载完成后才能复原本地数据
if _G.AppSetting.IS_EDITOR == true then
    local msgsimulatorwindow = CS.UnityEngine.Resources.FindObjectsOfTypeAll(_G.typeof(CS.Game.MessageSimulatorWindow))
    if _G.IsNil(msgsimulatorwindow) == false and msgsimulatorwindow.Length > 0 then
        local messagewindow = CS.UnityEditor.EditorWindow.GetWindow(_G.typeof(CS.Game.MessageSimulatorWindow))
        if _G.IsNil(messagewindow) == false then
            messagewindow:OnAllProtoLoadedCB()
        end
    end
end
