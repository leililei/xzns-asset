-----------------------------------------------------------------------
-- Created by wyx on 25. 十一月 2019 17:01
--
-- @Description C#侧派发事件在Lua端的响应
-----------------------------------------------------------------------
---@class CSDispatchEventManager : Singleton C#侧派发事件在Lua端的响应
---@field GetInstance fun():CSDispatchEventManager
-- local CSDispatchEventManager = _G.BaseClass("CSDispatchEventManager", _G.Singleton)
-- local CS = _G.CS
-- _G.CSDispatchEventManager = CSDispatchEventManager
-- local AddEvent = function(eName)
--     CS.YKEventLuaHelper.AddLuaCallEvent(eName)
-- end
-- local RemoveEvent = function(eName)
--     CS.YKEventLuaHelper.RemoveLuaCallEvent(eName)
-- end
-- function CSDispatchEventManager:__init()
--     self.EventNameCache = {}
--     self.OnceEventNameCache = {}
--     self.EventDic = {}
--     self.OnceEventDic = {}
--     ---此处通过Lua这边的事件再次派发
--     CS.YKEventLuaHelper.CSDispatchEventToLuaHandler = function(eName, ...)
--         for _, v in ipairs(self.EventDic) do
--             if v == eName then
--                 _G.Events:GetInstance():Broadcast(eName, ...)
--                 break
--             end
--         end
--         local index
--         for i, v in ipairs(self.OnceEventDic) do
--             if v == eName then
--                 index = i
--                 _G.Events:GetInstance():Broadcast(eName, ...)
--                 break
--             end
--         end
--         if index then
--             self.OnceEventNameCache[eName] = nil
--             table.remove(self.OnceEventDic, index)
--         end
--     end
-- end
-- ---AddEvent 添加事件监听
-- ---@param eName string 事件枚举，对应C#侧
-- ---@param isOnce boolean 是否值执行一次
-- ---@param isFirst boolean 是否值添加到队首
-- function CSDispatchEventManager:AddEvent(eName, isOnce, isFirst)
--     if isOnce == nil then
--         isOnce = false
--     end
--     if isFirst == nil then
--         isFirst = false
--     end
--     if not type(eName) == "string" then
--         _G.LogFatal("eName must be string")
--     end
--     local cache = isOnce and self.OnceEventNameCache or self.EventNameCache
--     local Dic = isOnce and self.OnceEventDic or self.EventDic
--     if not cache[eName] then
--         cache[eName] = eName
--         if isFirst then
--             -- Dic = tmp
--             local tmp = {}
--             table.insert(tmp, eName)
--             for _, v in ipairs(Dic) do
--                 table.insert(tmp, v)
--             end
--         else
--             table.insert(Dic, eName)
--         end
--         AddEvent(eName)
--     end
-- end
-- ---RemoveEvent 移除事件监听
-- ---@param eName string 事件枚举，对应C#侧
-- ---@param isOnce boolean 是否值执行一次
-- function CSDispatchEventManager:RemoveEvent(eName, isOnce)
--     if isOnce == nil then
--         isOnce = false
--     end
--     local cache = isOnce and self.OnceEventNameCache or self.EventNameCache
--     local Dic = isOnce and self.OnceEventDic or self.EventDic
--     local index
--     for i, v in ipairs(cache) do
--         if v == eName then
--             index = i
--             break
--         end
--     end
--     if cache[eName] then
--         cache[eName] = nil
--     end
--     if index then
--         table.remove(Dic, index)
--         RemoveEvent(eName)
--     end
-- end
