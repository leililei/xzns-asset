---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by user.wyx
--- DateTime: 2019/4/2 10:23
---创建一个计数器，一般用于枚举或者计数用
---

local Indexer = {}

function Indexer:GetCurIndex()
    return self.beginIndex
end

Indexer.New = function(defValue, step)
    local obj = {}
    obj.beginIndex = defValue or 0
    setmetatable(
        obj,
        {
            __index = Indexer,
            __call = function(...)
                obj.beginIndex = obj.beginIndex + (step or 1)
                return obj.beginIndex
            end
        }
    )
    return obj
end
_G.Indexer = Indexer

--[[
--Example:
local counter1 = Indexer.New()
local tbl = {}
table.insert(tbl,counter1())
table.insert(tbl,counter1())
table.insert(tbl,counter1())
table.insert(tbl,counter1())

local counter2 = Indexer.New(100,1)
table.insert(tbl,counter2())
table.insert(tbl,counter2())

for i,v in pairs(tbl) do
    print(i,v)
end

--result:
--1   1
--2   2
--3   101
--4   102
--]]
