-----------------------------------------------------------------------
-- D:.hzw.Client.Alpha.Client.Assets.Resources.LuaScripts.World.AI.Conditionals.PatrolConditional	(修改路径必须修正此项!)
-- Created by szs
-- DateTime: 2020/04/03
-- Description: 巡逻条件
-----------------------------------------------------------------------

---@class PatrolConditional	中文描述
local PatrolConditional = _G.BaseClass("PatrolConditional", _G.AINode)
_G.PatrolConditional = PatrolConditional

---类的构造函数
function PatrolConditional:OnStart()
    --todo 巡逻条件没有，不过好像也不会有
    ---这个值外围控制，暂时放这里
    self.Xl = true
end

function PatrolConditional:OnUpdate()
    ---是否列队
    if self.Xl then
        return _G.NodeStatus.Success
    end

    return _G.NodeStatus.Failure
end

---类的析构函数
function PatrolConditional:Dispose()
end
